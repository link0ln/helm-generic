apiVersion: v1
stringData:
  VAULT_ADDR: {{ .Values.address }}
  AVP_TYPE: vault
  AVP_AUTH_TYPE: token
  VAULT_TOKEN: {{ .Values.token }}
kind: Secret
metadata:
  name: {{ include "generic-service.fullname" . }}-vault-access
type: Opaque
